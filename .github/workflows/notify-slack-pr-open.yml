name: Slack PR Notification

on:
  workflow_call:
    secrets:
      SLACK_BOT_TOKEN:
        required: true
      SLACK_CHANNEL_ID:
        required: true

jobs:
  demodev-lab:
    runs-on: self-hosted
    steps:
      - name: Send Slack Notification - PR Opened
        continue-on-error: true
        uses: slackapi/slack-github-action@v2.1.1
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "${{ secrets.SLACK_CHANNEL_ID }}",
              "attachments": [
                {
                  "color": "#2196F3",
                  "fallback": "[${{ github.event.repository.name }}] PR이 생성되었어요. 리뷰가 필요해요",
                  "blocks": [
                    {
                      "type": "header",
                      "text": {
                        "type": "plain_text",
                        "text": "👀 PR이 생성되었어요. 리뷰가 필요해요",
                        "emoji": true
                      }
                    },
                    {
                      "type": "section",
                      "fields": [
                        {
                          "type": "mrkdwn",
                          "text": "*레포지토리:*\n`${{ github.repository }}`"
                        },
                        {
                          "type": "mrkdwn",
                          "text": "*번호:*\n#${{ github.event.pull_request.number }}"
                        },
                        {
                          "type": "mrkdwn",
                          "text": "*브랜치:*\n`${{ github.event.pull_request.head.ref }}` → `${{ github.event.pull_request.base.ref }}`"
                        },
                        {
                          "type": "mrkdwn",
                          "text": "*작성자:*\n${{ github.event.pull_request.user.login }}"
                        }
                      ]
                    },
                    {
                      "type": "section",
                      "text": {
                        "type": "mrkdwn",
                        "text": "*PR 보기:* <${{ github.event.pull_request.html_url }}|${{ github.event.pull_request.title }}>"
                      }
                    }
                  ]
                }
              ]
            }